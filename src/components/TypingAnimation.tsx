'use client'

import { ReactTyped, ReactTypedProps } from "react-typed"

function TypingAnimation(props: ReactTypedProps) {
  return (
    <ReactTyped {...props}></ReactTyped>
  )
}

export default TypingAnimation